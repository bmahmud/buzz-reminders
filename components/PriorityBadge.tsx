import { PriorityPill, type PriorityPillProps } from './ui/PriorityPill';

/** @deprecated Use PriorityPill directly */
export function PriorityBadge(props: PriorityPillProps) {
  return <PriorityPill {...props} />;
}
